[project]
name = "mcp-meta-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "openai",
    "httpx",
    "python-dotenv",
    "fastmcp>=3.3.1",
]

[tool.uv]
package = false
